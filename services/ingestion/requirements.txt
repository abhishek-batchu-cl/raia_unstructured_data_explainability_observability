fastapi>=0.109.0
uvicorn[standard]>=0.27.0
aiofiles>=23.2.0
prometheus-client>=0.19.0
pydantic>=2.5.0
jsonschema>=4.20.0

# Optional: Postgres support
asyncpg>=0.29.0

# Optional: Kafka support
aiokafka>=0.10.0

# EAEF SDK (for signature verification)
# In production, install from package
# For now, add parent directory to PYTHONPATH
